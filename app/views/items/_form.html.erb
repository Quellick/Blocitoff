<% if current_user %>
  <%= form_for [item], url: user_item_index_path(@user) do |f| %>
    <div class="form-group">
      <%= f.text_field :name, autofocus: true %>
    </div>
    <div class="form-group">
      <%= f.submit "Add item", class: 'btn btn-primary' %>
    </div>
  <% end %>
<% end %>